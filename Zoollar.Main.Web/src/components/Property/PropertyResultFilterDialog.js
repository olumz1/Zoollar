import { Dialog } from "@mui/material";

export default function PropertyResultFilterDialog(props) {
  return (
    <Dialog open={props.filterOpen} onClose={props.handleFilterClickClose}>
      To be continued
    </Dialog>
  );
}
